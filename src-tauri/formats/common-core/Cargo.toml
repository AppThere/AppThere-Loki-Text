[package]
name = "common-core"
version = "0.1.0"
edition = "2021"
description = "Format-agnostic document core types for AppThere Loki"
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
